# A GerencIA roda sem instalar nada: só Python 3.9 ou superior.
# Se PyYAML estiver presente, ele é usado para ler config/politica.yaml;
# se não estiver, espia/yaml_min.py assume o lugar sem diferença de resultado.
#
#   pip install -r requirements.txt   (opcional)

PyYAML>=6.0
